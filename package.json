{
  "name": "go-nogo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mission control game — Phase 0: The Countdown",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:graph": "node --experimental-strip-types tools/graphLint.ts src/data/causes.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@types/node": "^22.20.1",
    "svelte": "^5.56.10",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=22.6"
  }
}
